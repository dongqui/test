import * as THREE from 'three';
import _ from 'lodash';

interface FnDeleteKeyframeFromBase {
  bones: THREE.Bone[];
}

/**
 *
 */
const fnDeleteKeyframeFromBase = (props: FnDeleteKeyframeFromBase) => {};

export default fnDeleteKeyframeFromBase;
