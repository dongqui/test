import * as THREE from 'three';
import _ from 'lodash';

interface FnDeleteKeyframeFromLayer {
  bones: THREE.Bone[];
}

/**
 *
 */
const fnDeleteKeyframeFromLayer = (props: FnDeleteKeyframeFromLayer) => {};

export default fnDeleteKeyframeFromLayer;
