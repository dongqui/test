import { PlaskMocapData, PlaskRetargetMap } from 'types/common';

const retargetMocapData = (assetId: string, retargetMap: PlaskRetargetMap, mocapData: PlaskMocapData) => {
  console.log('mocapData: ', mocapData);
};

export default retargetMocapData;
