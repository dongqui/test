import * as BABYLON from '@babylonjs/core';
import { PlaskRetargetMap, SerializedBone } from 'types/common';
import { getRandomStringKey } from 'utils/common';

const DEFAULT_HIP_SPACE = 106;

const createRetargetMap = (assetId: string, serializeBones: SerializedBone[]) => {};

export default createRetargetMap;
