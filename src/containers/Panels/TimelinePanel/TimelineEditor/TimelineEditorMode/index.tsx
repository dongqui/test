import DopeSheet from './DopeSheet';

const TimelineEditorMode = () => {
  // 추후 state에 따라 dope sheet, curve editor, object view 출력여부 결정
  return <DopeSheet />;
};

export default TimelineEditorMode;
